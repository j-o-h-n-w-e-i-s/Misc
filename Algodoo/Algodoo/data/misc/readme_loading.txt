When Algodoo is loading, the image loading.png is shown.
Inside loading.png there is text shown; a title text and a message.

loading_text_rects.cfg descides where that text is put.
The first line describes the title, the second the message.

The coordinates are pixels inside loading.png. The order is:

font_size left top right bottom